{
  "name": "brandnewtodoap",
  "version": "1.0.0",
  "description": "TaskAppNew",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"cd frontend && npm start\" \"cd backend && npm run server\"",
    "client": "cd frontend && npm start",
    "server": "cd backend && npm run server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1"
  }
}
